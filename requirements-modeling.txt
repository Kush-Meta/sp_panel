# Modeling dependencies (in addition to requirements.txt).
# The gradient boosters are optional: sp_panel.evaluate degrades gracefully to the
# scikit-learn models if any are missing.
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
